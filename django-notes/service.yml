apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: apache
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80    # exposed port in the cluster
      targetPort: 80     # container port
  type: ClusterIP

